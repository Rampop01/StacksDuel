'use client';
export default function AdminPanel({ onResolve }: { onResolve: (id: string, winner: number) => void }) {
    return <div className="p-6 border border-rose-500/50 rounded-xl bg-black"><h2>Admin Resolution</h2></div>;
}